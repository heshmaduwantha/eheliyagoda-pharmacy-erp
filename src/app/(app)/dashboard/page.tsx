import { CheckCircle2, Database, KeyRound, ScrollText, Server, ShieldCheck, UsersRound } from "lucide-react";
import { requirePermission } from "@/modules/auth/permissions";

const statusCards = [
  { title: "Authentication", value: "Active", note: "Signed staff sessions", icon: KeyRound, tone: "teal" },
  { title: "Access roles", value: "2", note: "Owner and pharmacist", icon: UsersRound, tone: "blue" },
  { title: "PostgreSQL", value: "Ready", note: "Primary source of truth", icon: Database, tone: "emerald" },
  { title: "Audit foundation", value: "Ready", note: "Traceable server actions", icon: ScrollText, tone: "amber" },
] as const;

export default async function DashboardPage() {
  const user = await requirePermission("dashboard.view");
  const date = new Intl.DateTimeFormat("en-LK", { dateStyle: "full" }).format(new Date());

  return <div><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-bold text-teal-700">Welcome back, {user.name}</p><h1 className="mt-1 text-3xl font-black tracking-tight text-slate-900 sm:text-4xl">Foundation overview</h1><p className="mt-2 text-slate-500">Secure platform status before operational modules are introduced.</p></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-medium text-slate-600 shadow-sm">{date}</div></div><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{statusCards.map(({ title, value, note, icon: Icon, tone }) => <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_8px_30px_rgba(15,51,58,.05)]" key={title}><div className={`grid size-11 place-items-center rounded-2xl ${tone === "teal" ? "bg-teal-50 text-teal-700" : tone === "blue" ? "bg-blue-50 text-blue-700" : tone === "emerald" ? "bg-emerald-50 text-emerald-700" : "bg-amber-50 text-amber-700"}`}><Icon className="size-5" /></div><p className="mt-5 text-sm font-medium text-slate-500">{title}</p><p className="mt-1 text-2xl font-black text-slate-900">{value}</p><p className="mt-2 text-xs text-slate-500">{note}</p></article>)}</section><div className="mt-6 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_8px_30px_rgba(15,51,58,.05)]"><div className="flex items-center justify-between"><div><h2 className="text-lg font-black text-slate-900">Phase 1 capabilities</h2><p className="mt-1 text-sm text-slate-500">Foundation + Auth/RBAC completion status</p></div><ShieldCheck className="size-6 text-teal-600" /></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{["Secure login and logout", "Database-backed permissions", "Role-aware navigation", "Server-side route guards", "Validated environment", "Reusable audit service"].map((item) => <div className="flex items-center gap-3 rounded-xl bg-slate-50 p-3 text-sm font-semibold text-slate-700" key={item}><CheckCircle2 className="size-5 shrink-0 text-emerald-500" />{item}</div>)}</div></section><section className="overflow-hidden rounded-2xl bg-[linear-gradient(145deg,#075c63,#07957f)] p-6 text-white shadow-xl shadow-teal-900/10"><div className="flex items-center justify-between"><h2 className="text-lg font-black">System posture</h2><Server className="size-6 text-teal-200" /></div><div className="mt-8 flex items-center gap-4"><div className="relative grid size-20 place-items-center rounded-full border-8 border-white/15"><span className="text-2xl font-black">OK</span><span className="absolute inset-[-8px] rounded-full border-8 border-transparent border-t-teal-200" /></div><div><p className="font-bold">Foundation operational</p><p className="mt-1 text-sm leading-6 text-teal-50/70">Core services are configured. Business modules remain intentionally disabled.</p></div></div><div className="mt-8 h-16 opacity-80"><svg aria-hidden="true" className="h-full w-full text-teal-200" viewBox="0 0 400 70"><path d="M0 42h68l9-10 12 24 13-51 13 64 12-35 11 15 12-7h66l9-13 11 28 12-48 14 55 13-31 11 16 12-7h61" fill="none" stroke="currentColor" strokeLinecap="round" strokeWidth="3" /></svg></div></section></div></div>;
}
