export function ReportTable({ headers, rows, emptyMessage }: { headers: string[]; rows: string[][]; emptyMessage: string }) {
  return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_8px_30px_rgba(15,51,58,.05)]"><div className="overflow-x-auto"><table className="w-full min-w-[860px] border-collapse text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr>{headers.map((heading) => <th className="border-b border-slate-200 px-5 py-4 font-bold" key={heading}>{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row, rowIndex) => <tr className="hover:bg-teal-50/30" key={`${row[0]}-${rowIndex}`}>{row.map((value, columnIndex) => <td className={`px-5 py-4 ${columnIndex === 0 ? "font-semibold text-slate-800" : "text-slate-600"}`} key={`${columnIndex}-${value}`}>{value}</td>)}</tr>)}{rows.length === 0 ? <tr><td className="px-5 py-16 text-center text-slate-400" colSpan={headers.length}>{emptyMessage}</td></tr> : null}</tbody></table></div></section>;
}
